import 'package:flutter/material.dart';

class MoveCountScreen extends StatelessWidget {
  const MoveCountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MoveCountScreen — yakında')),
    );
  }
}
